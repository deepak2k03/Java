package others;
public class A {
    // we wanna access this outside the others package we have to make it public 
    public int x=8;
    // we can access this private things in same class only 
    private int z=10; 
    //defual access modifer used to acces on same package only
    int p=14;
    //protected can be accessed in same package and in a sub class of different packages
}
